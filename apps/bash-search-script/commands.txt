@author David Kirwan;
@description File containing useful commands it needs to be manually updated at the moment;
@useage @searchtag # command # description delimited by semicolon;

@ssh # /etc/init.d/ssh start # start the SSH Daemon;
@ssh #/etc/init.d/ssh stop # stop the SSH Daemon;
@ssh # sshd-generate # To perform the initial setp for sshd;
@tar # tar -zcvf name.tar.gz /directory # To add files to a zipped tarball;
@tar # tar -zxvf name.tar.gz # Untar/unzip tar.gz file;
@tr # tr -d '\000' < intervlan.txt > file2 # to remove all null chars from a file as its copied into file2;
@grep # grep patternToSearchFor file # Search a file for pattern;
@date @time # date +%s # prints out to the console the number of seconds since the epoch;
@useful # /etc/udev/rules.d/70-persistent-net.rules # file contains the mapping of interface name to hardware;
@apt-get # apt-cache search <string> # Search the apt-get repository for string;
@scp # scp *.txt user@remote.server.com:/home/user/ # copy *.txt to the remote server;
@scp # scp user@host:~/path/to/source/file /path/to/destination/file # Copy from remote to here;
@gpg # gpg --gen-key # Generates the PGP Key;
@gpg # gpg --armour --export my_email_address --output mykey.asc # Exports the PGP Key;
@gpg # gpg --import theirkey.asc # Imports the PGP Key;
@cron @crontab # crontab -e # Edit your crontab file, or create one if it doesn’t already exist.;
@cron @crontab # crontab -l # Display your crontab file.;
@cron @crontab # crontab -r # Remove your crontab file.;
@shortcut @application # gnome-desktop-item-edit ~/Desktop/ --create-new # Dialogbox for generating desktop shortcuts;
@git @log # git log # Prints out the information about who submitted the last revision;
@git @clone # git clone https://projects.lab.tssg.org/git/3mt # Clones the git repository to the current directory;
@grep # grep -r "<searchpattern>" * # Recursivly searches for <searchpattern> in files in the current directory;
@git @push # git push origin # Pushes your branch back upto the root;
@git @branch # git branch branchname # Creates a new branch;
@git @checkout # git checkout branchname # Switch to the named branch;
@clear @useful # ctrl + l # Clears the terminal screen;
@maven @setup # mvn archetype:generate -DarchetypeGroupId=org.apache.maven.archetypes -DgroupId=maven -DartifactId=test # Create project;
@PATH @path @binpath # export PATH=/usr/for/example-1.0/bin:$PATH # How to set a directory to the bin $PATH;
@nginx # sudo /usr/local/nginx/sbin/nginx -s stop # Stops the NGINX webserver;
@dpkg @deb @package # sudo dpkg -i /path/to/package # Installs a Debian .deb package;
@sudo @handy # sudo !! # Repeats the last command but this time includes sudo before it;
@printer @cups # sudo /etc/init.d/cups start # Launch the cups printer dameon stuff;
@printer @gui # system-config-printer # GUI for the printer daemon;
@generate @directory @listings @html # tree -H baseHREF # Very handy little command for generating html directory listings;
@mount @shares @network @cifs # sudo mount -t cifs -o username=uname,password=pass,uid=uname,gid=uname //shares/Backups /local/path # Command for mounting shares drive;
@R @install @packages # install.packages(pkgname) # Install packages in R;
@R @load @File # source("commands.R") # Load a script file in R;
@cron @script # bash -li -c /path/to/script # To run a script in crontab as if it was you running it;
@davfs @mount # sudo mount.davfs -o uid=linuxuser,gid=linuxgroup https://path.to.the/docs-dav/ /path/to/local/folder/docs-dav/ # Mount web-dav filesys;
@rails @ruby @mongo # --orm=mongo_mapper # When working with rails and mongodb, use this command if its crying about an orm;
